---
title: 12th April, 2026 – Trezo CLI overhaul
description: New interactive project scaffolding with remote package registry and streamlined template selection.
date: 2026-04-12
---

- Introduce new `trezo init` command with interactive package and template selection
- Add `-n, --name`, -p, --package`and`-t, --template` flags for non‑interactive scaffolding
- Add `trezo list` command to display all available packages and templates in a tree view
- Display post‑setup commands as actionable next steps after project creation
- Improve CLI reliability with graceful cancellation (`Ctrl+C`) and clear error messages
