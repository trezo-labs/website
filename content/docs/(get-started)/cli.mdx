---
title: Trezo CLI
description: The entry point for creating and configuring Trezo applications.
---

## Overview

Trezo CLI simplifies Web3 project setup. It handles:

- Network Package selection
- Project templates
- Post-setup commands

This saves you from manually configuring SDKs, dependencies, and project structure.

---

<Steps>

### Initialize Project

Run the CLI:

```bash
npx trezo init
```

You can also specify your project name after the `init` command.

---

### Global Installation

You can also install the CLI globally:

```bash
npm install -g trezo
```

After installing, you can run `trezo init` directly without using `npx`.

---

### Interactive Setup Flow

You’ll be guided through a series of prompts to configure your project.

```bash
████████╗██████╗ ███████╗███████╗ ██████╗      ██████╗██╗     ██╗
╚══██╔══╝██╔══██╗██╔════╝╚══███╔╝██╔═══██╗    ██╔════╝██║     ██║
   ██║   ██████╔╝█████╗    ███╔╝ ██║   ██║    ██║     ██║     ██║
   ██║   ██╔══██╗██╔══╝   ███╔╝  ██║   ██║    ██║     ██║     ██║
   ██║   ██║  ██║███████╗███████╗╚██████╔╝    ╚██████╗███████╗██║
   ╚═╝   ╚═╝  ╚═╝╚══════╝╚══════╝ ╚═════╝      ╚═════╝╚══════╝╚═╝

Welcome to Trezo CLI

? What package would you like to use?
> @trezo/evm – Ethereum
  @trezo/strk – Starknet
  @trezo/sui – Sui Network
  @trezo/sol – Solana

? Choose a template to start with:
  NextJs 16 template with a demo Todo dApp
> Vite template with a demo Todo dApp

? What would you like to name your project? (trezo-app)

✔ Project created successfully!
```

Each template includes a **demo Todo dApp** pre-integrated with your selected package.

---

### Post-Setup Commands

Some templates include additional setup commands:

```bash
The following post-setup commands will complete your project setup:

1. cd trezo-project
2. pnpm install

? Would you like to run these commands now? (Y/n)
```

- You can run them automatically from the CLI
- If skipped, run them manually later

</Steps>

---

## Generated Project

After setup, your project includes:

- Pre-configured Trezo integration
- Selected blockchain package installed
- Wallet connection support
- Project structure based on your template

Your project is ready for development — a fully working Web3 app out of the box.
