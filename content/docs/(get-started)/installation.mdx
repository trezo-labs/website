---
title: Installation
description: Scaffold a new Trezo project and automatically configure blockchain integrations.
---

<Steps>

### Quick Start

Run the command install Trezo and initialize a new project:

```bash
npx trezo init
```

For the full details and updated flow, see the [CLI documentation](/docs/cli).

---

### Core Packages

Install only the package for the blockchain you're building on.

<div className="my-8 grid gap-4 sm:grid-cols-2">
  <LinkedCard href="/docs/packages/evm">
    <figure className="flex flex-col items-center">
      <Image
        src="/packages/ethereum-eth-logo.svg"
        width="40"
        height="40"
        alt="Eth-logo"
        priority
        quality={100}
        className="size-10 border-none -mt-1"
      />
      <figcaption className="mt-2 font-medium text-sm">@trezo/evm</figcaption>
    </figure>
  </LinkedCard>
  <LinkedCard href="/docs/packages/strk">
    <figure className="flex flex-col items-center">
      <Image
        src="/packages/starknet-strk-logo.svg"
        width="40"
        height="40"
        alt="Starknet-logo"
        priority
        quality={100}
        className="size-10 border-none -mt-1"
      />
      <figcaption className="mt-2 font-medium text-sm">@trezo/strk</figcaption>
    </figure>
  </LinkedCard>
</div>

</Steps>

---

Each network package provides a type-safe integration layer, wallet connectivity, and contract interaction APIs — all preconfigured and ready to use.
