---
title: Installation
description: Scaffold a new Trezo project and automatically configure blockchain integrations.
---

## Quick Start

To install Trezo and initialize a new project, run the following command in an empty directory:

```bash
npx trezo init
```

For the full details and updated flow, see the [CLI documentation](/docs/cli).

---

## Packages

Each network package provides a type-safe integration layer, wallet connectivity, and contract interaction APIs — all preconfigured and ready to use.

<div className="my-8 grid gap-4 sm:grid-cols-2 sm:gap-6">
  <LinkedCard href="/docs/packages/evm">
    <figure className="flex flex-col items-center">
      <Image
        src="/packages/ethereum-eth-logo.svg"
        width="40"
        height="40"
        alt="Eth-logo"
        className="size-10 -mt-0.5 border-none"
      />
      <figcaption className="mt-3 font-medium">@trezo/evm - EVM</figcaption>
    </figure>
  </LinkedCard>
  <LinkedCard href="/docs/packages/strk">
    <figure className="flex flex-col items-center">
      <Image
        src="/packages/starknet-strk-logo.svg"
        width="40"
        height="40"
        alt="Eth-logo"
        className="size-10 -mt-0.5 border-none"
      />
      <figcaption className="mt-3 font-medium">
        @trezo/strk - Starknet
      </figcaption>
    </figure>
  </LinkedCard>
</div>

> Install only the package for the blockchain you're building on. Each package bundles its own dependencies, so a single install is all you need.
